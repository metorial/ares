import { createQueueProcessors } from '@lowerdeck/queue';

// Send queue processors (emails, notifications, etc.)
// TODO: Implement send queues when ready
export let sendQueues = createQueueProcessors([]);
